import React from 'react';
import { HashRouter, Route, Router } from "react-router-dom";
import About from "./router"

function App(){
  return <HashRouter>
    <Router />
  </HashRouter>
}

export default App